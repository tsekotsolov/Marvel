import React from "react";

import { LoaderWrapper, Loader } from "./StyledLoading";

const Loading = () => {
  return (
    <LoaderWrapper>
      <Loader />
    </LoaderWrapper>
  );
};

export default Loading;
